include "map.inc"
include "robot.inc"


# configure the GUI window
window
(
   size [700.000 700.000]
   scale 23
)

# load an environment bitmap

bui4floor
(
   bitmap "../bitmaps/autolab.png"
   size [30 30 0.5]
)

bigbob
(
		  name "bot1"
		  pose [ -7.22 -5.70 0 0]
		  color "green"
)